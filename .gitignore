# Xcode / SPM build artifacts
.build/
.swiftpm/
DerivedData/
build/

# Xcode user state
*.xcuserstate
xcuserdata/

# SwiftPM package resolution (keep Package.resolved only if dependencies are pinned; currently no deps)
# MacroMarkKit/.build/ and MacroMarkKit/.swiftpm/ are covered above

# macOS
.DS_Store

# Fastlane
fastlane/report.xml
fastlane/test_output/
fastlane/screenshots/
fastlane/metadata/      # uncomment once metadata is committed: !fastlane/metadata/

# Bundler
.bundle/
vendor/bundle/

# Scratch / local-only files
test.swift
